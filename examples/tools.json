[
  {
    "name": "search_docs",
    "description": "Search a documentation corpus and return matching passages.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "query": { "type": "string", "description": "Search query." },
        "limit": { "type": "integer", "description": "Max results." }
      },
      "required": ["query"]
    }
  },
  {
    "name": "get_weather",
    "description": "Return the current weather for a city (sample/defensive read-only tool).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "city": { "type": "string" },
        "units": { "type": "string", "enum": ["metric", "imperial"] }
      },
      "required": ["city"]
    }
  }
]
